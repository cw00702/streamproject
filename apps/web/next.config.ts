import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["livecloud-thumb.akamaized.net","nng-phinf.pstatic.net","ssl.pstatic.net"],
    
  },
};

export default nextConfig;
